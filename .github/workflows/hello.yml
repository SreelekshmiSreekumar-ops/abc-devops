name: First GitHub Action

on: [push]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Greet the world
        run: echo "👋 Hello, Sree! You just triggered a GitHub Action!"
